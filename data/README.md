# Dataset metadata

`dataset_example.csv` documents the metadata schema used by SVEET. It contains paths only.
Place the referenced files below your chosen `--dataset_base_path`, or create a new CSV using the
same columns. Do not commit third-party dataset videos unless their licenses explicitly permit it.
